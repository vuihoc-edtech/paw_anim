name: paw_anim
description: "Reusable paw reward animation widgets for VUIHOC."
version: 0.0.1
homepage:

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_svg:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  assets:
    - assets/cat_paws.svg
